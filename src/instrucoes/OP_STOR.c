#include "OP_STOR.h"

void op_stor_busca_operandos(BancoRegistradores *banco, Barramento *barramento, Memoria *memoria) {
    // funcao de busca de operandos
}

void op_stor_executar(int iteracao, BancoRegistradores *banco, ULA *ula) {
    // funcao de execucao
}

void op_stor_escrita_resultados(BancoRegistradores *banco, Barramento *barramento, Memoria *memoria, ULA *ula) {
    // funcao de escrita de resultados
}